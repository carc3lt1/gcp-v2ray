:8080 {
    # Enrutar directamente a Xray cualquier petición que use la ruta secreta del túnel
    handle /CARC3LT {
        reverse_proxy 127.0.0.1:10000
    }

    # Actice Probing Defense: Cualquier otro intento de acceso fuera de la ruta secreta va a Claro
    handle {
        redir https://paquetes.miclaro.com.gt 302
    }
}
